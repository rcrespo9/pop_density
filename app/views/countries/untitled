<% form_for @country do |f| %> 
	<div>
		<%= f.select(:Country, :name, Country.all.collect {|p| [ p.name, p.pop_density, p.country_id ] }) %>
	</div>
	<div>
		<%= f.submit %>
	</div>
<% end %>


<%= options_for_select(Country.all.collect {|p| [ p.name, p.id, { 'data-density' => p.pop_density}]})%>